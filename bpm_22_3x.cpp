#include <iostream>
#include <string>
int h,w,cws,cwc,a;

//cws против, cwc по часовой
int main()
{
    std::string h;
    std::string w;
    int arr[10];
    h = arr[10];
    w = arr[10];
    std::cin >> h >> w;
    a = 1; // a последнее число a = h*w - (h*w+1)
    if (h== w) {
        std::cout << arr[10];
    }
    else {
        std::cout << 0h << 00w;
    }

}

